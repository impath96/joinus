<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.joinus.mapper.MemberMapper">
	<select id="selectMember" resultType="MemberVo">
		select * from members where member_email = #{member_email};
	</select>
	<insert id="insertMember">
		insert into members (member_email, member_pass, member_name, member_image, member_type)
		values (#{member_email},#{member_pass},#{member_name},#{member_image},#{member_type})
	</insert>
</mapper>